import React from 'react';

const UserF = ({lastName}) => {
    // const {lastName} = props;
    return (
        <div>
             <h1>{lastName}</h1>
        </div>
    );
};

export default UserF;